# 部署
1. **重启服务**：
   ```bash
   pm2 restart poem-api
   ```
2. **广播**：
   ```bash
   npm run broadcast -- --version vx.x
   ```
# 改动
1. 新类型：天文地理。
2. 查重。
点击锁定，会切换为锁定状态，但此时点击保存会显示编辑和锁定两行。再点编辑进入编辑模式，还是两行。如果在编辑状态下保存，显示编辑状态的内容。修改成无论什么情况下保存，都只显示锁定状态的