# 部署
1. **重启服务**：
   ```bash
   pm2 restart poem-api
   ```
2. **广播新版本**：
   ```bash
   npm run broadcast -- --version vx.x
   ```
# 改动
1. 新类型：词牌名，分类（单双调，曲调），起源，正体（字数、例词、平仄、叶韵），变体（同正体）。
4. 查重。
6. 新类型：天文地理。
7. 符号、空格自检。